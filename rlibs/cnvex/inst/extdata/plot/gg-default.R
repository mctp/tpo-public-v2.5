OPTS <- list(
    ## global
    point.per.segment=Inf,
    abs.range=12,
    box.oddcol="#e3e3e3",
    box.evencol="white",
    ## grid plot
    grid.max.seg.cols=12,
    ## lr plot
    lr.chr.names=TRUE,
    lr.chr.yoff=c(0.05,0.05),
    lr.chr.prefix="",
    lr.chr.col="#4f7bb0",
    lr.col.type="CK",
    lr.col.wt="black",
    lr.range.dynamic=TRUE,
    lr.range.limit=c(-4,4),
    lr.lines=c(0,1,2,3,4,6,8),
    lr.lines.col="black",
    lr.lines.size=0.25,
    lr.lines.alpha=1,
    lr.off.target=FALSE,
    ## baf plot
    baf.chr.names=TRUE,
    baf.chr.yoff=c(0.05, 0.05),
    baf.chr.prefix="",
    baf.chr.col="#4f7bb0",
    baf.col.type="CK",
    baf.col.wt="black",
    baf.range.dynamic=TRUE,
    baf.range.limit=c(0,1),
    baf.lines=list(M=c(0,1),C=c(1,3)),
    baf.lines.lab="lab.cdiff",
    baf.lines.col="red",
    baf.lines.size=0.25,
    baf.lines.alpha=1
)
